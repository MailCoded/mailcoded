<?xml version="1.0" encoding="utf-8"?>
<!-- No package sources: restore resolves entirely from the global packages folder.
     Use when nuget.org is unreachable and every package is already cached. -->
<configuration>
  <packageSources>
    <clear />
  </packageSources>
</configuration>
